import { render } from '@testing-library/react'

import { {{pascalCase (extractFilename path)}} } from '.'

test('renders {{pascalCase (extractFilename path)}} component', () => {
    render(<{{pascalCase (extractFilename path)}} />)
})
